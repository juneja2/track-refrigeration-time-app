<div class="container">
    <h1 class="display-3 my-2">Welcome to the Track Refrigeration Time App.</h1>

    <div>
      <ul>
        <li>Location Id [1, 6] inclusive are refrigerators.</li>
        <li>Location Id [7, 12] inclusive are not refrigerators.</li>
        <li>Reset will make total time spent outside a refrigerator equal to 0.</li>
        <li>Location Id's remain active even during the reset.</li>
      </ul>
    </div>

    <%= form_with url: update_out_time_path, class: "my-2", method: :post do |form| %>
        <%= form.label :item_id, "Item ID" %>
        <%= form.number_field :item_id %>
        <%= form.label :location_id, "Location ID" %>
        <%= form.number_field :location_id %>
        <%= form.label :timestamp, "Timestamp(in seconds)" %>
        <%= form.number_field :timestamp %>
        <%= form.submit "Update Out Time", class: "btn btn-primary" %>
    <% end %>

    <hr />
    <%= form_with url: get_out_time_path, class: "my-2", method: :get do |form| %>
        <%= form.label :item_id, "Item ID" %>
        <%= form.number_field :item_id %>
        <%= form.submit "Get Out Time", class: "btn btn-primary" %>
    <% end %>

    <hr />
    <%= form_with url: reset_path, class: "my-2", method: :post do |form| %>
        <%= form.submit "Reset", class: "btn btn-primary" %>
    <% end %>
</div>
